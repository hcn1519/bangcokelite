<div class="modal fade" id="forgot_pass" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
  <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :role => 'form'}) do |f| %>
     <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 style="margin-top:0px;margin-bottom: 0px;">비밀번호 찾기</h4>
    <p>비밀번호 재설정 이메일을 보내드립니다.</p>
    </div>
    <div class="modal-body">
    <%= devise_error_messages! %>
    <div class="form-group">
      <%= f.label :email, "이메일" %>
      <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
    </div>
    <div class="modal-footer">
     <%= f.submit '비밀번호 변경', :class => 'btn btn-primary right' %>
     </div>
  <% end %>
    
    </div>
  </div>
</div>