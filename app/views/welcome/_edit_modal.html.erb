<div class="modal fade" id="edit" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4>회원정보 수정</h4>
      </div>
      <div class="modal-body">
        <%= form_for(resource, :as => resource_name, :url =>  edit_user_registration_path(resource_name), :html => { :method => :put, :role => 'form'}) do |f| %>
        
          <%= devise_error_messages! %>
          <div class="form-group">
            <%#= f.label :name,"이름" %>
            <%= f.text_field :name, :autofocus => true,class: 'form-control getname', placeholder:"이름" %>
          </div>
          <div class="form-group">
            <%#= f.label :email,"이메일" %>
            <%= f.email_field :email, class: 'form-control getemail', placeholder:"이메일" %>
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>
          </div>
          <fieldset style="padding: 10px; background-color: #ECF0F1; border-radius: 10px;">
            <p style="font-size:0.8em">비밀번호 바꾸기를 원하지 않으면, 아래 칸을 그대로 두세요.</p>
            <div class="form-group">
              <%= f.password_field :password, :autocomplete => 'off', class: 'form-control', placeholder: "새로운 비밀번호" %>
            </div>
            <div class="form-group">
              <%= f.password_field :password_confirmation, class: 'form-control', placeholder: "한 번 더" %>
            </div>
          </fieldset>
          <fieldset style="margin-top:20px; padding: 10px; background-color: #ECF0F1; border-radius: 10px;">
            <p style="font-size:0.8em">정보 수정을 위해서는 현재 비밀번호를 입력해야합니다.</p>
            <div class="form-group">
              <%= f.password_field :current_password, class: 'form-control', placeholder: "현재 비밀번호" %>
            </div>
          </fieldset>
          <%= f.submit '업데이트', :class => 'btn btn-primary right', :style => 'float:right; margin-top:10px; clear:left' %>
        <% end %>
      </div>
     
      <div class="form-group" style="padding-left:15px; padding-right:15px; margin-top:45px; margin-bottom:70px">
        <h4>회원탈퇴</h3>
        <p style="font-size:0.8em">탈퇴하면 슬플꺼에요..</p>
        <%= button_to "회원탈퇴하기", registration_path(resource_name), :data => { :confirm => "정말요..?" }, :method => :delete, :class => 'btn btn-danger', :style => 'float:right;' %>
      </div>
     
    </div>
  </div>
</div>

<script>
 $(document).ready(function() {
   
 });
</script>