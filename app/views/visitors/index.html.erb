<h3>Welcome</h3>
<% if user_signed_in? %>
    <p><%= link_to 'User', users_path %> <%= User.count %> registered</p>
<% else %>
    <p><%= link_to "User", "#login", "data-toggle" => "modal" %> <%= User.count %> registered</p>
<% end %>

